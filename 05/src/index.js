import React from "react";
import ReactDOM from "react-dom/client";
import ReactExample from "./RouterExample";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <ReactExample />
  </React.StrictMode>
);
